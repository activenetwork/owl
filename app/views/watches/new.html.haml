%h2 New watch

- form_for(@watch) do |f|
  = render :partial => 'form', :locals => { :f => f }
  .save
    = f.submit 'Create and Add Another'
    = f.submit 'Create'
    or
    = link_to 'Cancel', :back
